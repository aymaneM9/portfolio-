import { motion } from "framer-motion";
import { styles } from "../styles";
import { ComputersCanvas } from "./canvas";

const Hero2 = () => {
  return (
    <section className="">
      {/* Canvas stays full screen and interactive */}
      <ComputersCanvas />
    </section>
  );
};

export default Hero2;
